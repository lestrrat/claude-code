# Reporting

- When working in a git worktree branch and reporting back to the user, you MUST always include the worktree name (e.g. `.worktrees/<branch-name>`) in the report so the user knows which worktree the work was performed in.
