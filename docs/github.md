# GitHub

## Tool

- ALWAYS use the `gh` CLI for GitHub interactions (issues, PRs, releases, checks, etc.).
- NEVER scrape GitHub web pages or construct API URLs manually.

## Caching

See `~/.claude/docs/remote-data.md` — applies to all `gh` output.
