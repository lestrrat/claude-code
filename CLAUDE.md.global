<!-- Agent-consumed file. Keep terse, unambiguous, machine-parseable. Optimize for token efficiency. -->

# Shell — CRITICAL

**One command per Bash call. No compound commands. No exceptions.**

## Banned Syntax

These are FORBIDDEN in ALL Bash calls. Violations break the permission allowlist.

| Banned | Examples | Use instead |
|--------|----------|-------------|
| `&&` `\|\|` | `cd dir && make` | Separate sequential Bash calls |
| `\|` (pipe) | `cmd \| grep x` | Run cmd, use Grep tool on output |
| `>>` `<` | `cmd >> file` | Write tool |
| `$(...)` backticks | `echo $(date)` | Separate Bash call, capture result |
| `;` with `cd` | `cd dir; make` | Separate sequential Bash calls |
| `for` `while` `until` `if/then` `case` | loops/conditionals | Multiple parallel tool calls |

`>` is allowed ONLY for writing to `.tmp/` files. `;` is allowed ONLY between non-`cd` commands.

## Dedicated Tools Over Shell

ALWAYS use dedicated tools. Bash is ONLY for commands with no tool equivalent.

| Task | Use | NOT |
|------|-----|-----|
| Read files | `Read` tool | `cat`/`head`/`tail` |
| Search content | `Grep` tool | `grep`/`rg` |
| Find files | `Glob` tool | `find`/`ls` |
| Edit files | `Edit` tool | `sed`/`awk` |
| Write files | `Write` tool | `echo`/`cat` with redirection |

## Other

- `head`/`tail`: MUST use `-n N`. NEVER `head -N` / `tail -N`.
- Run commands in another directory → `cd <dir>` as its own Bash call, then the command as a separate Bash call. `cd` back afterward.

# Temp Files

- NEVER use `/tmp/`. Use `$PROJECT_DIR/.tmp/`.
- NEVER commit `.tmp/` or its contents to git.
- Periodically clean up `.tmp/`: delete files older than 7 days or when total size exceeds 50 MB (largest files first).

# Worktrees

ALWAYS create a worktree BEFORE editing any code. NEVER work in the root checkout. Each worktree MUST branch from a fresh, up-to-date main branch — fetch first, then branch from `origin/main` (or the repo's primary branch). NEVER branch one worktree off another. See `~/.claude/docs/git-operations.md` for commands and naming conventions.

# Code Reviews

Not-applicable review feedback → add inline code comment explaining why the concern doesn't apply. Prevents same comment in future reviews.

# Pre-Read Rules

Read linked doc BEFORE any work in that area. No exceptions.

| Area | Trigger | Doc |
|------|---------|-----|
| Git | commit, push, add, branch, merge, rebase, worktree, etc. | `~/.claude/docs/git-operations.md` |
| Go | Writing Go code | `~/.claude/docs/go.md` |
| GitHub | Issues, PRs, checks, releases | `~/.claude/docs/github.md` |
| Remote | gh, curl, wget, kubectl, cloud CLIs, etc. | `~/.claude/docs/remote-data.md` |
| Report | Reporting results to user | `~/.claude/docs/reporting.md` |
| Commit msg | formatting commit messages | `~/.claude/docs/commit-messages.md` |
| Agent docs | writing/editing CLAUDE.md, docs/, skills/ | `~/.claude/docs/agent-instructions.md` |
