# claude-code

Personal configuration for [Claude Code](https://claude.ai/code). Contains global instructions, reference docs, skills, hooks, and settings.

## Setup

```bash
bash setup.sh
```

Creates symlinks in `~/.claude/` pointing to this repo. `CLAUDE.md.global` is linked as `~/.claude/CLAUDE.md`; everything else keeps its original name.
